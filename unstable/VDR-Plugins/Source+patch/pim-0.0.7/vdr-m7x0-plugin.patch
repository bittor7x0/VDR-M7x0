diff -Naur -x .svn pim-0.0.7/calEvent.c /home/garrulo/VDR-EM-SVN-plugins2/VDR-NG/PLUGINS/src/pim/calEvent.c
--- pim-0.0.7/calEvent.c	2008-07-08 20:27:36.000000000 +0200
+++ /home/garrulo/VDR-EM-SVN-plugins2/VDR-NG/PLUGINS/src/pim/calEvent.c	2008-09-20 12:08:50.000000000 +0200
@@ -22,6 +22,7 @@
 #include <string>
 #include <iostream>
 
+#include <stdlib.h>
 #include "i18n.h"
 #include "calEvent.h"
 
diff -Naur -x .svn pim-0.0.7/Makefile /home/garrulo/VDR-EM-SVN-plugins2/VDR-NG/PLUGINS/src/pim/Makefile
--- pim-0.0.7/Makefile	2008-07-08 20:27:36.000000000 +0200
+++ /home/garrulo/VDR-EM-SVN-plugins2/VDR-NG/PLUGINS/src/pim/Makefile	2008-09-17 19:45:37.000000000 +0200
@@ -16,13 +16,13 @@
 ### The C++ compiler and options:
 
 CXX      ?= g++
-CXXFLAGS ?= -g -Wall -Woverloaded-virtual
+CXXFLAGS ?= -fPIC -g -Wall -Woverloaded-virtual
 
 ### The directory environment:
 
-DVBDIR = ../../../../DVB
-VDRDIR = ../../..
-LIBDIR = ../../lib
+DVBDIR ?= ../../..
+VDRDIR ?= ../../..
+LIBDIR ?= ../../lib
 TMPDIR = /tmp
 
 ### Allow user defined options to overwrite defaults:
@@ -58,7 +58,7 @@
 
 # Dependencies:
 
-MAKEDEP = g++ -MM -MG
+MAKEDEP = $(CXX) -MM -MG
 DEPFILE = .dependencies
 $(DEPFILE): Makefile
 	@$(MAKEDEP) $(DEFINES) $(INCLUDES) $(OBJS:%.o=%.c) > $@
@@ -71,7 +71,7 @@
 
 libvdr-$(PLUGIN).so: $(OBJS)
 	$(CXX) $(CXXFLAGS) -shared $(OBJS) -o $@
-	@cp $@ $(LIBDIR)/$@.$(APIVERSION)
+	@cp -f $@ $(LIBDIR)/$@.$(APIVERSION)
 
 dist: clean
 	@-rm -rf $(TMPDIR)/$(ARCHIVE)
